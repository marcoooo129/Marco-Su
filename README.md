# Marco Su — Personal website

A trilingual personal portfolio in Chinese, English and Italian. Built with React, TypeScript, Tailwind CSS and Motion.

## Local development

```bash
npm install
npm run dev
```

Create a production build with `npm run build`.
